
---
title: "Azure VM Lab - Part D: Develop & Deploy J2EE Web Application (Eclipse IDE)"
description: "Complete guide to develop and deploy a Student Registration J2EE web application using Eclipse IDE, JSP, and SQL Server on Azure VM. Connects HTML forms to database with JDBC."
tags: ["azure", "virtual-machine", "eclipse", "j2ee", "jsp", "servlet", "jdbc", "sql-server", "tomcat", "web-application"]
date: 2024-02-10
modified: 2024-12-01
---

# PART D — Develop & Deploy Web Application (Eclipse IDE)
## Complete Beginner-Friendly Professional Lab Manual

**All steps in this part run INSIDE the Azure VM via RDP (Desktop 2).**

**Time Required:** 20–35 minutes

**What you will build:** A Student Registration web application using JSP that connects to SQL Server, inserts student records via an HTML form, and displays all records in a styled table. The application runs on Apache Tomcat inside the Azure VM and is accessible from your laptop's browser.

**Before starting, confirm:**
```
✅ Part A complete (Azure VM running, RDP connected)
✅ Part B complete (SQL Server 2022 installed, StudentDB created, port 1433 open)
✅ Part C complete (JDK 17 installed, Tomcat 10.1 running on port 8080)
✅ You are inside the VM via RDP (Desktop 2)
✅ Internet works inside the VM
✅ Notepad on Desktop 1 has all credentials
✅ SQL Server service is running (port 1433)
✅ Tomcat service is running (port 8080)
```

---

## Version Lock for Part D

```
┌──────────────────────────┬─────────────────────────────────────────┐
│ Component                │ Exact Version                           │
├──────────────────────────┼─────────────────────────────────────────┤
│ Eclipse IDE              │ 2024-09 (Enterprise Java & Web)         │
│ Eclipse Package          │ eclipse-jee-2024-09-R-win32-x86_64.zip  │
│ JDK (for Eclipse)        │ Eclipse Temurin 17.0.13 (from Part C)  │
│ Tomcat (in Eclipse)      │ Apache Tomcat 10.1.34 (from Part C)    │
│ JDBC Driver              │ mssql-jdbc-12.8.1.jre17.jar            │
│ Dynamic Web Module       │ 5.0                                     │
│ JSP Files                │ index.jsp, displayStudents.jsp          │
└──────────────────────────┴─────────────────────────────────────────┘
```

> **Important:** Eclipse IDE for **Enterprise Java and Web Developers** is required. The regular "Eclipse IDE for Java Developers" does NOT include JSP/Servlet/Web support. We will verify the correct package before downloading.

---

## D1 — Download Eclipse IDE for Enterprise Java

### What to Do

**Inside the VM (Desktop 2 via RDP):**

```
Open Edge:
→ Start Menu → Type: edge → Press: Enter
```

**Navigate to Eclipse download page:**

```
Click address bar: Ctrl + L
Type:  https://www.eclipse.org/downloads/packages/
Press: Enter
```

### What You See

A page listing multiple Eclipse packages.

```
┌─────────────────────────────────────────────────────────────┐
│  Eclipse Downloads                                          │
│                                                             │
│  Get Eclipse IDE xxxxx                                      │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Eclipse IDE for Java Developers                   │    │
│  │  xxx MB                                            │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Eclipse IDE for Enterprise Java and Web Developers│    │
│  │  xxx MB                                       ← THIS│    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌────────────────────────────────────────────────────┐    │
│  │  Eclipse IDE for C/C++ Developers                  │    │
│  │  ...                                               │    │
│  └────────────────────────────────────────────────────┘    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Find the CORRECT Package ⭐ CRITICAL

**Scroll down** to find:

```
Eclipse IDE for Enterprise Java and Web Developers
```

> **NOT** "Eclipse IDE for Java Developers" — that one does NOT support JSP/Servlet/Web projects.

**Click on: "Eclipse IDE for Enterprise Java and Web Developers"**

### Select Windows x64 Download

```
┌─────────────────────────────────────────────────────────────┐
│  Eclipse IDE for Enterprise Java and Web Developers         │
│                                                             │
│  Download Links                                             │
│                                                             │
│  Windows x86_64     [ Download ]                            │
│  Linux x86_64       [ Download ]                            │
│  macOS x86_64       [ Download ]                            │
│  macOS AArch64      [ Download ]                            │
│                                                             │
│  Select Another Mirror                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Click: "Download"** next to **Windows x86_64**

```
File:  eclipse-jee-2024-09-R-win32-x86_64.zip
Size:  ~400 MB
```

**Wait for download to complete** (5–15 minutes depending on internet speed).

### Possible Error D1.1: Page shows "2024-12" or "2025-03" instead of "2024-09"

```
Why:    Eclipse releases new versions every quarter.
        2024-09 may have been superseded by a newer release.

Fix:    → Download whatever version is shown
        → This guide works for ANY 2024-x or 2025-x release
        → The package name "Enterprise Java and Web Developers" is what matters
        → Note the exact version in Notepad
```

### Possible Error D1.2: Download is very slow or fails

```
Why:    Eclipse download mirrors can be slow, especially from certain regions.

Fix:    Step 1: Click "Select Another Mirror" on the download page
        → Choose a mirror geographically close to you
        → Click the download link from that mirror
        
        Step 2: If all mirrors are slow
        → Download on your LAPTOP instead:
           → Desktop 1 → Open browser → Download Eclipse from the same URL
           → Transfer to VM using OneDrive:
              → Upload Eclipse ZIP to OneDrive from laptop
              → Inside VM: Open Edge → onedrive.live.com
              → Download the ZIP from OneDrive inside the VM
        
        Step 3: If download keeps failing
        → Try downloading at a different time (off-peak hours)
        → Or: Use a direct mirror link:
           https://mirror.csclub.uwaterloo.ca/eclipse/technology/epp/downloads/release/
           → Navigate to the latest release folder
           → Download the win32-x86_64.zip file
```

### Possible Error D1.3: Edge blocks the download

```
Why:    SmartScreen may flag the download.

Fix:    → Look for a notification bar at the top or bottom of Edge
        → Click: "..." → "Keep" or "Download anyway"
        → If asked "Do you want to keep this file?" → Click "Keep"
```

### Verify Download

```
File Explorer: Win + E
Navigate to: C:\Users\azureadmin\Downloads
Look for: eclipse-jee-2024-09-R-win32-x86_64.zip
Size: ~400 MB
```

```
✅ CHECKPOINT: Eclipse IDE ZIP downloaded (~400 MB)
   → In Notepad (Desktop 1): "✅ D1 — Eclipse IDE downloaded"
```

---

## D2 — Extract Eclipse to C:\

### What to Do

**Inside the VM:**

```
Navigate to: C:\Users\azureadmin\Downloads
Right-click: eclipse-jee-2024-09-R-win32-x86_64.zip
Click: Extract All...
```

### Extraction Dialog

```
┌─────────────────────────────────────────────────────────────┐
│  Extract Compressed (Zipped) Folders                        │
│                                                             │
│  Files will be extracted to this folder:                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  C:\Users\azureadmin\Downloads\eclipse-jee-2024-09- │   │
│  │  R-win32-x86_64                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ☑ Show extracted files when complete                      │
│                                                             │
│            [ Extract ]    [ Cancel ]                         │
└─────────────────────────────────────────────────────────────┘
```

**Change the extraction path to `C:\`:**

```
Click: "Browse" (or edit the path directly)
Change to:  C:\
```

```
Click: Extract
```

**Wait 1–3 minutes** for extraction (400 MB compressed → ~800 MB uncompressed).

### Verify Extraction

**Open Command Prompt:**

```cmd
dir C:\eclipse\eclipse.exe
```

**Expected:**
```
 Directory of C:\eclipse

xx/xx/xxxx  xx:xx AM    xxx,xxx    eclipse.exe
               1 File(s)       xxx,xxx bytes
```

### Possible Error D2.1: "Access denied" when extracting to C:\

```
Why:    File Explorer doesn't have permission to write to C:\ root.

Fix:    
    Option A: Extract to a user folder first, then move
        → Extract to: C:\Users\azureadmin\Downloads\
        → Open File Explorer
        → Navigate to: C:\Users\azureadmin\Downloads\eclipse
        → Right-click the "eclipse" folder → Cut (Ctrl + X)
        → Navigate to: C:\
        → Paste (Ctrl + V)
        → If prompted for admin permission → Click "Continue"
    
    Option B: Use Command Prompt
        → Open CMD as Admin
        → Run:
           mkdir C:\eclipse
           xcopy "C:\Users\azureadmin\Downloads\eclipse-jee-2024-09-R-win32-x86_64\eclipse\*" "C:\eclipse\" /E /I /H
```

### Possible Error D2.2: Folder is named differently

```
Why:    The ZIP may extract to a different folder name.

Fix:    → Check what was extracted to C:\
        → Look for a folder containing eclipse.exe
        → Common names: eclipse, eclipse-jee, eclipse-jee-2024-09
        → Note the exact folder name
        → If needed, rename to "eclipse" for consistency:
           → Right-click → Rename → type: eclipse
        
        → Use YOUR actual folder name in all subsequent steps
```

### Possible Error D2.3: Nested folder structure

```
Why:    Some ZIP tools create a parent folder.

Fix:    → If you have: C:\eclipse-jee-2024-09\eclipse\eclipse.exe
        → Move the inner eclipse folder to C:\
        → Result should be: C:\eclipse\eclipse.exe
        
        → Or: C:\eclipse\eclipse\eclipse.exe
        → Move the inner eclipse folder to replace the outer one
```

```
✅ CHECKPOINT: Eclipse extracted to C:\eclipse\ (eclipse.exe exists)
   → In Notepad: "✅ D2 — Eclipse extracted to C:\eclipse"
```

---

## D3 — Launch Eclipse and Set Workspace

### What to Do

**Inside the VM:**

```
Open File Explorer: Win + E
Navigate to: C:\eclipse
Double-click: eclipse.exe
```

### UAC Prompt (If Appears)

```
Click: Yes
```

### Possible Error D3.1: "A Java Runtime Environment (JRE) or Java Development Kit (JDK) must be available to run Eclipse"

```
┌─────────────────────────────────────────────────────────────┐
│  A Java Runtime Environment (JRE) or Java Development Kit   │
│  (JDK) must be available to run Eclipse. No Java virtual    │
│  machine was found after searching the following locations: │
│  ...                                                        │
└─────────────────────────────────────────────────────────────┘

Why:    Eclipse can't find the JDK installed in Part C.

Fix:    
    Step 1: Verify JDK is installed
        → Open NEW Command Prompt
        → Run: java -version
        → If it works: JDK is installed, Eclipse just can't find it
        → If it fails: Go back to Part C and fix JDK installation
    
    Step 2: Tell Eclipse where JDK is
        → Open Notepad inside the VM
        → Open the file: C:\eclipse\eclipse.ini
        → Add these two lines BEFORE the line that starts with "-vmargs":
        
        -vm
        C:\Program Files\Eclipse Adoptium\jdk-17.0.13.11-hotspot\bin\javaw.exe
        
        → Replace the path with YOUR exact JDK path from Part C
        → Save the file (Ctrl + S)
        → Try launching eclipse.exe again
        
        → IMPORTANT: The "-vm" line and the path MUST be on SEPARATE lines
        → The path must point to javaw.exe (not java.exe)
        → There must be no extra spaces or characters
        
        → Your eclipse.ini should look like:
        
        ...
        --launcher.appendVmargs
        -vm
        C:\Program Files\Eclipse Adoptium\jdk-17.0.13.11-hotspot\bin\javaw.exe
        -vmargs
        ...
```

### Possible Error D3.2: Eclipse starts but shows "Java was started but returned exit code=13"

```
Why:    Architecture mismatch — Eclipse is 64-bit but JDK is 32-bit,
        or vice versa.

Fix:    → Verify JDK is 64-bit:
        → CMD: java -version → Should say "64-Bit"
        → If it says 32-bit: Uninstall and install the 64-bit JDK
        
        → Verify Eclipse is 64-bit:
        → Downloaded file should be "x86_64" (64-bit)
        → If you downloaded the wrong one, re-download
        
        → Both must be 64-bit for this to work
```

### Possible Error D3.3: "An error has occurred. See the log file..."

```
Why:    Various Eclipse startup errors.

Fix:    → Note the log file path shown in the error
        → Open the log file with Notepad
        → Look for the first "Caused by:" line
        
        → Common fix: Delete Eclipse workspace metadata
        → Delete: C:\workspace-j2ee\.metadata (if it exists from a previous attempt)
        → Re-launch eclipse.exe
        
        → If still failing:
        → Close all other programs inside the VM
        → VM may be low on memory (1 GB for B2s)
        → Restart the VM → Reconnect RDP → Launch Eclipse immediately
```

### Workspace Selection Dialog

```
┌─────────────────────────────────────────────────────────────┐
│  Workspace Launcher                                         │
│                                                             │
│  Select a directory as workspace                            │
│                                                             │
│  Eclipse uses the workspace directory to store your         │
│  preferences and projects.                                  │
│                                                             │
│  Workspace:                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  C:\Users\azureadmin\eclipse-workspace              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ☐ Use this as the default and do not ask again             │
│                                                             │
│            [ Launch ]    [ Cancel ]                          │
└─────────────────────────────────────────────────────────────┘
```

**Change the workspace path to:**

```
C:\workspace-j2ee
```

**How to change it:**
```
Click inside the Workspace text box
Select all text (Ctrl + A)
Type: C:\workspace-j2ee
```

```
Click: Launch
```

### Eclipse Loading Screen

```
Wait 30–60 seconds for Eclipse to fully load.
This is normal on first launch — Eclipse builds its workspace.
```

### Welcome Tab

```
┌─────────────────────────────────────────────────────────────┐
│  Eclipse IDE                                     [×]        │
│                                                             │
│  ┌─ Welcome ─────────────────────────────────────────────┐ │
│  │                                                        │ │
│  │  Welcome to Eclipse                                    │ │
│  │  ...                                                   │ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  [Package Explorer] [Servers] [Problems] [Console]          │
└─────────────────────────────────────────────────────────────┘
```

**Close the Welcome tab:** Click the `×` on the "Welcome" tab.

**You should now see the empty Eclipse workspace.**

```
✅ CHECKPOINT: Eclipse IDE launched and workspace set
   → In Notepad: "✅ D3 — Eclipse launched, workspace: C:\workspace-j2ee"
```

---

## D4 — Configure Tomcat Server in Eclipse

### What to Do

**In Eclipse (inside the VM):**

```
Top Menu → Window → Preferences
```

### Navigate to Server Runtime Environments

```
┌─────────────────────────────────────────────────────────────┐
│  Preferences                                                │
│                                                             │
│  🔍 Type filter text...                                     │
│                                                             │
│  ▶ General                                                  │
│  ▶ Ant                                                      │
│  ▶ Data Management                                         │
│  ▶ Help                                                     │
│  ▶ Java                                                     │
│  ▶ ...                                                      │
│  ▶ Server                              ← EXPAND THIS        │
│     ▶ Runtime Environments              ← CLICK THIS        │
│                                                             │
│                          [ Apply and Close ] [ Cancel ]     │
└─────────────────────────────────────────────────────────────┘
```

**Expand "Server"** (click the ▶ arrow)
**Click: "Runtime Environments"**

### Add Tomcat Server

```
┌─────────────────────────────────────────────────────────────┐
│  Preferences > Server > Runtime Environments                │
│                                                             │
│  Configured server runtime environments:                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  (empty — no servers configured yet)                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│            [ Add... ]  [ Edit... ]  [ Remove ]              │
│                                                             │
│                          [ Apply and Close ] [ Cancel ]     │
└─────────────────────────────────────────────────────────────┘
```

**Click: Add...**

### Select Server Type

```
┌─────────────────────────────────────────────────────────────┐
│  New Server                                                  │
│                                                             │
│  Select the type of server to create:                       │
│                                                             │
│  ▶ Apache                                                   │
│     ▶ Apache Tomcat v8.5                                    │
│     ▶ Apache Tomcat v9.0                                    │
│     ▶ Apache Tomcat v10.0                                   │
│     ▶ Apache Tomcat v10.1               ← SELECT THIS       │
│     ▶ Apache Tomcat v11.0                                   │
│  ▶ ...                                                      │
│                                                             │
│  ☑ Create a new local server                                │
│                                                             │
│            [ Next > ]    [ Cancel ]                          │
└─────────────────────────────────────────────────────────────┘
```

**Expand "Apache"** (click the ▶ arrow)

**Click: "Apache Tomcat v10.1"**

**Verify:** ☑ "Create a new local server" is checked.

```
Click: Next
```

### Configure Tomcat Installation

```
┌─────────────────────────────────────────────────────────────┐
│  New Server > Tomcat Server                                 │
│                                                             │
│  Name:                                                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Apache Tomcat v10.1                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Tomcat installation directory:                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  C:\apache-tomcat-10.1.34                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  JRE:                                                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  jdk-17 (default)                                  ▼  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│            [ Finish ]    [ Cancel ]                          │
└─────────────────────────────────────────────────────────────┘
```

**Fill in:**

```
Name:  Leave as: Apache Tomcat v10.1

Tomcat installation directory:
    → Type: C:\apache-tomcat-10.1.34
    → Or click "Browse" and navigate to: C:\apache-tomcat-10.1.34

JRE:
    → Should show: jdk-17 (or similar)
    → If not: See Error D4.1 below
```

### Possible Error D4.1: JRE dropdown is empty or doesn't show JDK 17

```
Why:    Eclipse doesn't know where JDK 17 is.

Fix:    Step 1: Click the JRE dropdown → Select "jdk-17"
        → If it appears: Select it → Continue
        
        Step 2: If dropdown doesn't show jdk-17
        → Click: "Installed JREs..." (link near the JRE dropdown)
        → A new dialog opens:
        
        ┌───────────────────────────────────────────────┐
        │  Installed JREs                                │
        │                                                │
        │  Name          │ Location                      │
        │  ──────────────────────────────────────────── │
        │  (may be empty or show a wrong JRE)            │
        │                                                │
        │       [ Add... ] [ Edit ] [ Remove ]           │
        └───────────────────────────────────────────────┘
        
        → Click: Add...
        → Select: Standard VM → Click: Next
        → JRE Home: Click "Directory..."
           → Navigate to: C:\Program Files\Eclipse Adoptium\jdk-17.0.13.11-hotspot
           → (Replace with YOUR exact JDK path)
           → Click: Select Folder (or OK)
        → JRE Name: Should auto-fill as "jdk-17.0.13.11-hotspot" or similar
        → Click: Finish
        → ☑ Check the checkbox next to the new JRE in the list
        → Click: Apply and Close
        
        → Back in the Tomcat server dialog:
        → JRE dropdown should now show the JDK
        → Select it
```

### Possible Error D4.2: "The Tomcat installation directory is not valid"

```
Why:    Eclipse can't find required Tomcat files in the specified directory.

Fix:    Step 1: Verify the path is correct
        → File Explorer → Navigate to: C:\apache-tomcat-10.1.34
        → Must contain: bin\, conf\, lib\, webapps\, work\ folders
        → Must contain: bin\catalina.bat (this is what Eclipse checks)
        
        Step 2: If catalina.bat is missing
        → You may have downloaded the wrong Tomcat variant
        → Re-download: apache-tomcat-10.1.34-windows-x64.zip
        → Re-extract to C:\
        → Verify bin\catalina.bat exists
        → Try again
        
        Step 3: If path has extra nesting
        → Check: C:\apache-tomcat-10.1.34\apache-tomcat-10.1.34\bin\
        → If so: Move the inner folder up to C:\
        → Final structure: C:\apache-tomcat-10.1.34\bin\catalina.bat
```

### Possible Error D4.3: "Apache Tomcat v10.1" option not visible in server type list

```
Why:    Eclipse version may not include Tomcat 10.1 support.

Fix:    → Try "Apache Tomcat v10.0" instead
        → The configuration is nearly identical
        → It still works with Tomcat 10.1.34 (Eclipse just uses a slightly
           different internal mapping)
        
        → If no Apache options appear at all:
        → You downloaded the wrong Eclipse package
        → Must be: "Enterprise Java and Web Developers"
        → NOT: "Java Developers" or "C/C++ Developers"
        → Re-download the correct package (see D1)
```

```
Click: Finish
```

### Close Preferences

```
Click: Apply and Close
```

### Show Servers Panel

```
Top Menu → Window → Show View → Servers
```

**A "Servers" tab appears at the bottom of Eclipse:**

```
┌─────────────────────────────────────────────────────────────┐
│  [Servers] [Console] [Problems] [Search]                    │
│                                                             │
│  Tomcat v10.1 Server at localhost [Stopped, Republish]     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

```
✅ CHECKPOINT: Tomcat v10.1 configured in Eclipse
   → In Notepad: "✅ D4 — Tomcat configured in Eclipse"
```

---

## D5 — Download JDBC Driver

### What is the JDBC Driver?

JDBC (Java Database Connectivity) Driver is a `.jar` file that allows Java applications to connect to SQL Server. Without it, our web application cannot talk to the database.

### What to Do

**Inside the VM**, open Edge:

```
Click address bar: Ctrl + L
Type:  https://learn.microsoft.com/en-us/sql/connect/jdbc/download-microsoft-jdbc-driver-for-sql-server
Press: Enter
```

### Find the Download

**Scroll down** to find **"Microsoft JDBC Driver 12.x for SQL Server"**:

```
┌─────────────────────────────────────────────────────────────┐
│  Microsoft JDBC Driver 12.8 for SQL Server                  │
│                                                             │
│  sqljdbc_12.8.1.0_enu.exe                                   │
│  sqljdbc_12.8.1.0_enu.tar.gz                                │
│                                                             │
│  Click one of the links to download.                        │
└─────────────────────────────────────────────────────────────┘
```

**Click: `sqljdbc_12.8.1.0_enu.exe`**

> **Note:** Version numbers may differ (12.6, 12.8, 12.9, etc.). Download whatever 12.x version is shown.

```
File:  sqljdbc_12.8.1.0_enu.exe
Size:  ~2.5 MB
```

### Possible Error D5.1: Page shows JDBC Driver 12.6 or 12.9 instead of 12.8

```
Why:    Microsoft updates the JDBC driver periodically.

Fix:    → Download whatever 12.x version is available
        → ALL 12.x versions work with SQL Server 2022 and JDK 17
        → Note the exact version in Notepad
        → The important thing: Download the 12.x line, NOT 11.x or 10.x
```

### Extract the JDBC Driver

**Run the downloaded file:**

```
Navigate to: C:\Users\azureadmin\Downloads
Double-click: sqljdbc_12.8.1.0_enu.exe
```

### UAC Prompt

```
Click: Yes
```

### License Agreement

```
┌─────────────────────────────────────────────────────────────┐
│  Microsoft JDBC Driver for SQL Server                       │
│                                                             │
│  License Agreement                                          │
│                                                             │
│  ☐ I accept the terms...                                    │
│                                                             │
│  Directory:                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  C:\JDBC                                             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│            [ Unzip ]    [ Cancel ]                           │
└─────────────────────────────────────────────────────────────┘
```

```
Check: ☑ I accept the terms in the License Agreement
Change directory to: C:\JDBC
Click: Unzip
```

**Wait 5–10 seconds.**

```
Click: OK (when extraction completes)
```

### Find the Correct JAR File

**Open File Explorer → Navigate to:**

```
C:\JDBC\sqljdbc_12.8\enu\
```

> **Note:** Folder name may differ based on the exact version (e.g., `sqljdbc_12.6\enu\` or `sqljdbc_12.9\enu\`).

**You should see multiple JAR files:**

```
mssql-jdbc-12.8.1.jre11.jar    ← NOT this one (for JDK 11)
mssql-jdbc-12.8.1.jre17.jar    ← THIS ONE (for JDK 17) ✅
mssql-jdbc-12.8.1.jre8.jar     ← NOT this one (for JDK 8)
```

**We need the `jre17` version** because we installed JDK 17.

### Possible Error D5.2: Only jre8 or jre11 JARs are present (no jre17)

```
Why:    Older JDBC driver versions may not include a jre17 JAR.

Fix:    → Check if there's a jre11 JAR: mssql-jdbc-12.x.x.jre11.jar
        → This works with JDK 17 (JDK 17 is backward compatible with JRE 11)
        → Use the jre11 JAR instead
        
        → If only jre8 is present:
        → Download a newer JDBC driver version (12.4+)
        → Or: Use the jre8 JAR (may work but less ideal)
```

```
✅ CHECKPOINT: JDBC Driver extracted, mssql-jdbc-12.8.1.jre17.jar found
   → In Notepad: "✅ D5 — JDBC Driver downloaded and extracted to C:\JDBC"
```

---

## D6 — Create Dynamic Web Project in Eclipse

### What to Do

**In Eclipse (inside the VM):**

```
Top Menu → File → New → Dynamic Web Project
```

### Possible Error D6.1: "Dynamic Web Project" option not visible in the File → New menu

```
Why:    Wrong Eclipse package, or perspective not set.

Fix:    Step 1: Click "File → New → Other..." (at the bottom of the New menu)
        → In the wizard, expand: "Web"
        → Look for "Dynamic Web Project"
        → If found: Select it → Next
        
        Step 2: If "Web" category doesn't exist
        → You downloaded the wrong Eclipse package
        → Must be: "Enterprise Java and Web Developers"
        → NOT: "Java Developers"
        → Re-download the correct Eclipse package (see D1)
        → Re-extract and re-launch
        
        Step 3: If Web category exists but Dynamic Web Project is missing
        → You may need to switch to the Java EE perspective:
           → Top Menu → Window → Perspective → Open Perspective → Other...
           → Select: "Java EE" or "Web" → Open
        → Try File → New → Dynamic Web Project again
```

### Project Creation Dialog

```
┌─────────────────────────────────────────────────────────────┐
│  New Dynamic Web Project                                    │
│                                                             │
│  Project name:                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  StudentWebApp                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Project location:                                          │
│  ☑ Use default location                                     │
│    C:\workspace-j2ee\StudentWebApp                          │
│                                                             │
│  Target runtime:                                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Apache Tomcat v10.1                              ▼  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Dynamic web module version:                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  5.0 (Jakarta EE 9)                              ▼  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Configuration:                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Default Configuration                             ▼  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│            [ Finish ]  [ Next > ]  [ Cancel ]               │
└─────────────────────────────────────────────────────────────┘
```

**Fill in:**

```
Project name:           StudentWebApp
Project location:       ☑ Use default location (checked)
Target runtime:         Apache Tomcat v10.1  (from dropdown)
Dynamic web module:     5.0  (from dropdown)
Configuration:          Default Configuration
```

### Possible Error D6.2: "Target runtime" dropdown is empty

```
Why:    Tomcat was not configured as a runtime (Step D4 may have been skipped or failed).

Fix:    → Click: "New Runtime..." button (near the Target runtime dropdown)
        → Follow the same steps as D4:
           → Apache → Apache Tomcat v10.1 → Next
           → Installation directory: C:\apache-tomcat-10.1.34
           → JRE: jdk-17
           → Finish
        → The Target runtime dropdown now shows: Apache Tomcat v10.1
```

### Possible Error D6.3: "Dynamic web module version" doesn't show 5.0

```
Why:    Eclipse version may show different options.

Fix:    → Select the highest version available (5.0, 4.0, or 3.1)
        → 5.0 is ideal (Jakarta EE 9+, matches Tomcat 10.1)
        → 4.0 also works (javax → jakarta namespace mapping handled by Tomcat 10.1)
        → 3.1 is acceptable for our simple JSP application
```

```
Click: Next
```

### Java Source Page

```
┌─────────────────────────────────────────────────────────────┐
│  New Dynamic Web Project > Java Source                      │
│                                                             │
│  Source folder:                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  src/main/java                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [ Add Folder... ]  [ Edit... ]  [ Remove ]                 │
│                                                             │
│            [ < Back ]  [ Finish ]  [ Next > ]  [ Cancel ]   │
└─────────────────────────────────────────────────────────────┘
```

**Leave defaults.** The source folder `src/main/java` is correct.

```
Click: Next
```

### Web Module Page

```
┌─────────────────────────────────────────────────────────────┐
│  New Dynamic Web Project > Web Module                       │
│                                                             │
│  ☑ Generate web.xml deployment descriptor                   │
│                                                             │
│  Content directory:                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  src/main/webapp                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│            [ < Back ]  [ Finish ]  [ Cancel ]               │
└─────────────────────────────────────────────────────────────┘
```

**Verify:**
```
☑ Generate web.xml deployment descriptor    ← MUST be checked
Content directory: src/main/webapp
```

### Possible Error D6.4: "Generate web.xml" checkbox is not visible or grayed out

```
Why:    Eclipse UI variation.

Fix:    → If not visible, don't worry — we'll create web.xml manually later
        → If grayed out: Leave it — Eclipse may create it automatically
        → Click Finish
```

```
Click: Finish
```

### Perspective Switch Dialog (If Shown)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Open Associated Perspective?                               │
│                                                             │
│  This kind of project is associated with the Java EE        │
│  perspective. Do you want to open this perspective now?     │
│                                                             │
│            [ Yes ]    [ No ]    [ Remember my decision ]     │
└─────────────────────────────────────────────────────────────┘

Click: Yes
```

### Verify Project Structure

**In Eclipse Project Explorer (left panel):**

```
StudentWebApp
├── Deployment Descriptor: StudentWebApp
├── Java Resources/
│   └── src/main/java/          (empty — we don't need Java classes)
├── JavaScript Resources/
└── src/
    └── main/
        └── webapp/
            └── WEB-INF/
                └── web.xml
```

```
✅ CHECKPOINT: Dynamic Web Project "StudentWebApp" created
   → In Notepad: "✅ D6 — Dynamic Web Project created"
```

---

## D7 — Add JDBC Driver to Project

### What to Do

**In Eclipse Project Explorer:**

```
Expand: StudentWebApp → src → main → webapp → WEB-INF → lib
```

> **If `lib` folder doesn't exist:** Right-click on `WEB-INF` → New → Folder → Name: `lib` → Finish

**Now copy the JDBC JAR file into this folder.**

### Method A: Copy-Paste in File Explorer

```
Open File Explorer (Win + E)

Source:      C:\JDBC\sqljdbc_12.8\enu\mssql-jdbc-12.8.1.jre17.jar
Destination: C:\workspace-j2ee\StudentWebApp\src\main\webapp\WEB-INF\lib\

Copy the JAR file → Paste into the lib folder
```

**After pasting, go back to Eclipse:**

```
Right-click on "StudentWebApp" (project root in Project Explorer)
→ Click: Refresh   (or press F5)
```

**The JAR should now appear in the lib folder:**

```
StudentWebApp
└── src/main/webapp/WEB-INF/lib/
    └── mssql-jdbc-12.8.1.jre17.jar   ✅
```

### Method B: Import in Eclipse

```
Right-click on "lib" folder in Project Explorer
→ Import... → General → File System → Next
→ Browse to: C:\JDBC\sqljdbc_12.8\enu\
→ ☑ Check: mssql-jdbc-12.8.1.jre17.jar
→ Finish
```

### Possible Error D7.1: "Access denied" when pasting the JAR

```
Why:    Eclipse has a lock on the project files.

Fix:    → Close Eclipse temporarily
        → Copy the JAR file via File Explorer
        → Re-open Eclipse
        → Right-click StudentWebApp → Refresh (F5)
```

### Possible Error D7.2: JAR file not showing up in Eclipse after paste

```
Why:    Eclipse hasn't refreshed its file system view.

Fix:    → Right-click StudentWebApp → Refresh (F5)
        → If still not visible: Close and reopen Eclipse
        → Verify via File Explorer that the file exists:
           C:\workspace-j2ee\StudentWebApp\src\main\webapp\WEB-INF\lib\mssql-jdbc-12.8.1.jre17.jar
```

### Possible Error D7.3: Wrong JAR file selected (jre8 or jre11 instead of jre17)

```
Why:    You copied the wrong JAR from the JDBC folder.

Fix:    → Delete the wrong JAR from the lib folder
        → Copy the correct one: mssql-jdbc-12.8.1.jre17.jar
        → The filename MUST contain "jre17"
        → If no jre17 version exists, use jre11 (backward compatible)
        → Refresh Eclipse
```

```
✅ CHECKPOINT: JDBC Driver JAR in WEB-INF/lib
   → mssql-jdbc-12.8.1.jre17.jar visible in Eclipse Project Explorer
   → In Notepad: "✅ D7 — JDBC Driver added to project"
```

---

## D8 — Create index.jsp (Registration Form)

### What is index.jsp?

This is the main page of our web application. It displays an HTML form where users enter student details (name, email, department). When the form is submitted, it sends the data to `displayStudents.jsp` for processing.

### What to Do

**In Eclipse Project Explorer:**

```
Right-click on: src/main/webapp
→ New → JSP File
```

### New JSP File Dialog

```
┌─────────────────────────────────────────────────────────────┐
│  New JSP File                                                │
│                                                             │
│  Enter or select the parent folder:                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  StudentWebApp/src/main/webapp                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  File name:                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  index.jsp                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Select a JSP template:                                     │
│  ○ New JSP File (html)                                      │
│  ○ New JSP File (xml)                                       │
│  ◉ New JSP File (html)           ← Default, keep this       │
│                                                             │
│            [ Finish ]    [ Cancel ]                          │
└─────────────────────────────────────────────────────────────┘
```

```
Parent folder:  StudentWebApp/src/main/webapp  (should be auto-filled)
File name:      index.jsp
Template:       New JSP File (html)

Click: Finish
```

### Eclipse Opens index.jsp in the Editor

**You'll see default JSP content.** Select ALL content in the editor (`Ctrl + A`) and **delete it**.

**Then paste this entire code:**

```jsp
<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Student Registration</title>
</head>
<body style="font-family:Arial;padding:40px;background:#f0f0f0">

<div style="width:400px;margin:auto;background:#fff;padding:30px;border:1px solid #ccc">

    <h2 style="color:#2c5364;text-align:center">Student Registration</h2>

    <form action="displayStudents.jsp" method="post">

        <label>First Name:</label><br>
        <input type="text" name="firstName" required style="width:100%;padding:8px;margin:5px 0 15px"><br>

        <label>Last Name:</label><br>
        <input type="text" name="lastName" required style="width:100%;padding:8px;margin:5px 0 15px"><br>

        <label>Email:</label><br>
        <input type="email" name="email" required style="width:100%;padding:8px;margin:5px 0 15px"><br>

        <label>Department:</label><br>
        <select name="department" required style="width:100%;padding:8px;margin:5px 0 15px">
            <option value="" disabled selected>-- Select --</option>
            <option>Computer Science</option>
            <option>Electronics</option>
            <option>Mechanical</option>
            <option>Civil</option>
            <option>Electrical</option>
            <option>Information Technology</option>
        </select><br>

        <input type="submit" value="Register & View Records"
               style="width:100%;padding:10px;background:#2c5364;color:#fff;border:none;cursor:pointer;font-size:15px">

    </form>

    <p style="text-align:center;margin-top:15px">
        <a href="displayStudents.jsp">View All Records</a>
    </p>

</div>

</body>
</html>
```

### Save the File

```
Press: Ctrl + S
```

### Possible Error D8.1: JSP file shows red error markers

```
Why:    Eclipse may show syntax warnings for JSP files.

Fix:    → If the errors are just "warnings" (yellow): Ignore them
        → If there are red errors:
           → Hover over the error marker to see the message
           → Common: "The method getJspApplicationContext() is undefined"
              → This means the Tomcat library isn't in the build path
              → Fix: Right-click StudentWebApp → Properties → Java Build Path
                → Libraries tab → Add Library → Server Runtime
                → Select "Apache Tomcat v10.1" → Finish
                → Apply and Close
```

### Possible Error D8.2: Eclipse can't create the JSP file (option missing)

```
Why:    The "JSP File" option may not appear if the project isn't configured as a web project.

Fix:    → Verify the project has "Deployment Descriptor" in Project Explorer
        → If not visible: You may have created a plain Java project instead
        → Solution: Delete the project and recreate as Dynamic Web Project (see D6)
```

```
✅ CHECKPOINT: index.jsp created and saved
   → In Notepad: "✅ D8 — index.jsp created"
```

---

## D9 — Create displayStudents.jsp (Records Display)

### What is displayStudents.jsp?

This page does two things:
1. If the user submitted the form → inserts the student record into SQL Server
2. Always → queries all student records from SQL Server and displays them in an HTML table

### What to Do

**In Eclipse Project Explorer:**

```
Right-click on: src/main/webapp
→ New → JSP File
```

```
File name:  displayStudents.jsp
Click: Finish
```

**Select ALL content** (`Ctrl + A`) → **Delete it**.

**Paste this entire code:**

```jsp

<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ page import="java.sql.*" %>
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Student Records</title>
</head>
<body style="font-family:Arial;padding:40px;background:#f0f0f0">

<div style="width:800px;margin:auto;background:#fff;padding:30px;border:1px solid #ccc">

    <h2 style="color:#2c5364;text-align:center">Student Database Records</h2>

<%
    String url  = "jdbc:sqlserver://localhost:1433;databaseName=StudentDB;encrypt=true;trustServerCertificate=true;";
    String user = "sa";
    String pass = "P@ssw0rd123!";

    String fn = request.getParameter("firstName");
    String ln = request.getParameter("lastName");
    String em = request.getParameter("email");
    String dp = request.getParameter("department");
    int total = 0;
    Connection conn = null;

    try {
        Class.forName("com.microsoft.sqlserver.jdbc.SQLServerDriver");
        conn = DriverManager.getConnection(url, user, pass);

        // INSERT if form was submitted
        if (fn != null && !fn.trim().isEmpty()) {
            PreparedStatement ps = conn.prepareStatement(
                "INSERT INTO Students(FirstName,LastName,Email,Department) VALUES(?,?,?,?)");
            ps.setString(1, fn.trim());
            ps.setString(2, ln.trim());
            ps.setString(3, em.trim());
            ps.setString(4, dp.trim());
            ps.executeUpdate();
            ps.close();
%>
    <p style="color:green;text-align:center;font-weight:bold">
        Student "<%=fn%> <%=ln%>" registered successfully!
    </p>
<%
        }

        // COUNT total
        ResultSet cnt = conn.createStatement().executeQuery("SELECT COUNT(*) FROM Students");
        if (cnt.next()) total = cnt.getInt(1);
        cnt.close();
%>
    <p style="text-align:center">Total Students: <b><%=total%></b></p>

    <table border="1" cellpadding="10" cellspacing="0" style="width:100%;border-collapse:collapse">
        <tr style="background:#2c5364;color:#fff">
            <th>ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Email</th>
            <th>Department</th>
        </tr>
<%
        // SELECT all records
        ResultSet rs = conn.createStatement().executeQuery("SELECT * FROM Students ORDER BY StudentID DESC");
        while (rs.next()) {
%>
        <tr>
            <td><%=rs.getInt("StudentID")%></td>
            <td><%=rs.getString("FirstName")%></td>
            <td><%=rs.getString("LastName")%></td>
            <td><%=rs.getString("Email")%></td>
            <td><%=rs.getString("Department")%></td>
        </tr>
<%
        }
        rs.close();
    } catch (Exception e) {
%>
    <p style="color:red;text-align:center;font-weight:bold">Error: <%=e.getMessage()%></p>
<%
    } finally {
        if (conn != null) try { conn.close(); } catch (Exception x) {}
    }
%>
    </table>

    <br>
    <p style="text-align:center">
        <a href="index.jsp">Register Another Student</a>
        &nbsp;|&nbsp;
        <a href="displayStudents.jsp">Refresh Records</a>
    </p>

    <p style="text-align:center;color:#aaa;font-size:12px;margin-top:20px">
        Azure VM | J2EE | Tomcat 10.1.34 | SQL Server 2022
    </p>

</div>

</body>
</html>

```

### Save the File

```
Press: Ctrl + S
```

### Verify Project Structure

**In Eclipse Project Explorer, your project should now look like:**

```
StudentWebApp
├── Deployment Descriptor: StudentWebApp
├── Java Resources/
│   └── src/main/java/
├── JavaScript Resources/
└── src/
    └── main/
        └── webapp/
            ├── index.jsp                  ✅
            ├── displayStudents.jsp        ✅
            └── WEB-INF/
                ├── web.xml
                └── lib/
                    └── mssql-jdbc-12.8.1.jre17.jar   ✅
```

```
✅ CHECKPOINT: Both JSP files created and saved
   → In Notepad: "✅ D9 — displayStudents.jsp created"
```

---

## D10 — Configure Build Path (If Needed)

### What to Do

**If you see red error markers** on your JSP files, the Tomcat libraries may not be in the build path.

```
Right-click on: StudentWebApp (project root)
→ Properties
→ Java Build Path
→ Libraries tab
```

**Check if "Apache Tomcat v10.1" appears in the list.**

**If NOT:**

```
Click: Add Library...
→ Select: Server Runtime → Next
→ Select: Apache Tomcat v10.1 → Finish
```

```
Click: Apply and Close
```

**After adding the library, clean the project:**

```
Top Menu → Project → Clean...
→ ☑ Clean all projects
→ Click: Clean
```

```
✅ CHECKPOINT: Build path configured (if needed)
   → In Notepad: "✅ D10 — Build path verified"
```

---

## D11 — Stop Tomcat Windows Service

### Why This Step?

The Tomcat Windows service (from Part C) is already running on port 8080. Eclipse starts its own Tomcat instance on the same port. This causes a conflict. We need to stop the Windows service so Eclipse can use port 8080.

### What to Do

**Open Command Prompt as Administrator inside the VM:**

```cmd
net stop Tomcat10
```

**Expected:**
```
The Apache Tomcat 10.1 Tomcat10 service was stopped successfully.
```

### Possible Error D11.1: "The service is not started"

```
Why:    Tomcat Windows service was already stopped.

Fix:    → That's fine — proceed to the next step
        → No action needed
```

### Possible Error D11.2: "Access denied"

```
Why:    Command Prompt not running as Administrator.

Fix:    → Close current CMD
        → Right-click CMD → Run as administrator
        → UAC: Yes
        → Retry: net stop Tomcat10
```

```
✅ CHECKPOINT: Tomcat Windows service stopped
   → Port 8080 is now free for Eclipse to use
   → In Notepad: "✅ D11 — Tomcat service stopped"
```

---

## D12 — Run the Application on Server

### ⭐ THE MAIN STEP — This Deploys and Runs the Application

### What to Do

**In Eclipse Project Explorer:**

```
Right-click on: StudentWebApp (the project root — NOT a subfolder)
→ Run As → Run on Server
```

### Run on Server Dialog

```
┌─────────────────────────────────────────────────────────────┐
│  Run on Server                                              │
│                                                             │
│  Select the server that you want to use:                    │
│                                                             │
│  ◉ Choose an existing server                                │
│     ▶ localhost                                             │
│        ▶ Apache Tomcat v10.1 at localhost [Stopped]         │
│                                                             │
│  ○ Manually define a new server                             │
│                                                             │
│  ☑ Always use this server when running this project         │
│                                                             │
│  ☑ Use the server currently in use for this project         │
│                                                             │
│            [ Finish ]    [ Cancel ]                          │
└─────────────────────────────────────────────────────────────┘
```

**Verify:**
```
◉ Choose an existing server is selected
Under localhost: Apache Tomcat v10.1 at localhost is selected
☑ Always use this server when running this project
```

```
Click: Finish
```

### What Happens Next

Eclipse will:
```
1. Clean and build the StudentWebApp project
2. Package the JSP files, web.xml, and JDBC driver automatically
3. Deploy the application to Tomcat's webapps folder
4. Start the Tomcat server (inside Eclipse, not the Windows service)
5. Open the application URL in Eclipse's built-in browser
```

**Wait 15–60 seconds.** The first startup takes longer because Tomcat needs to initialize.

### What You Should See

**The Eclipse built-in browser opens showing:**

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Student Registration                                       │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  First Name                                         │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │                                               │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                     │   │
│  │  Last Name                                          │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │                                               │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                     │   │
│  │  Email                                              │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │                                               │  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                     │   │
│  │  Department                                         │   │
│  │  ┌───────────────────────────────────────────────┐  │   │
│  │  │ -- Select --                                 ▼│  │   │
│  │  └───────────────────────────────────────────────┘  │   │
│  │                                                     │   │
│  │  [  Register & View Records  ]                      │   │
│  │                                                     │   │
│  │  View All Records →                                 │   │
│  │                                                     │   │
│  │  [J2EE] [JSP] [SQL Server] [Tomcat 10.1] [Azure]   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**The registration form is displayed. This means the application is running!**

### Possible Error D12.1: "Port 8080 is already in use"

```
┌─────────────────────────────────────────────────────────────┐
│  Several ports (8080, 8005) are already in use by           │
│  another process.                                           │
└─────────────────────────────────────────────────────────────┘

Why:    The Tomcat Windows service is still running (you didn't stop it),
        or another program is using port 8080.

Fix:    Step 1: Stop the Windows service
        → Open CMD (Admin):
           net stop Tomcat10
        → If "service not started": The service is already stopped
        → Go to Step 2
        
        Step 2: Check what's using port 8080
        → CMD: netstat -ano | findstr "8080"
        → If output shows a PID: Another program uses the port
        → Kill it: taskkill /PID XXXX /F
        → Try Run on Server again
        
        Step 3: If nothing seems to be using the port
        → Eclipse may have cached the error
        → Clean the project: Project → Clean → Clean all
        → Try Run on Server again
        
        Step 4: Change Eclipse Tomcat port (last resort)
        → Double-click "Tomcat v10.1 Server at localhost" in Servers panel
        → Change HTTP/1.1 Port from 8080 to 8081
        → Save (Ctrl + S)
        → Try Run on Server again
        → Access at: http://localhost:8081/StudentWebApp/
```

### Possible Error D12.2: "The selection did not contain any resources that can run on a server"

```
Why:    You right-clicked the wrong item (e.g., a file inside the project instead of the project root).

Fix:    → In Project Explorer, right-click directly on "StudentWebApp" (the top-level project name)
        → NOT on a subfolder or file
        → Run As → Run on Server
```

### Possible Error D12.3: "Server Tomcat v10.1 was unable to start within 45 seconds"

```
Why:    The VM is slow (only 1 GB RAM for B2s) and Tomcat couldn't finish starting in time.

Fix:    
    Step 1: Increase the timeout
        → In the Servers panel (bottom of Eclipse):
        → Double-click "Tomcat v10.1 Server at localhost"
        → A configuration editor opens
        → Find "Timeouts" section (scroll down)
        → Change "Start (in seconds)" from 45 to 120
        → Save: Ctrl + S
        → Try Run on Server again
    
    Step 2: Free up VM memory
        → Close SSMS if open (it uses a lot of RAM)
        → Close other programs inside the VM
        → Try Run on Server again
    
    Step 3: Restart Eclipse
        → File → Restart
        → Try Run on Server again
```

### Possible Error D12.4: HTTP 404 — "The requested resource is not available"

```
Why:    Eclipse didn't deploy the application correctly, or the URL is wrong.

Fix:    Step 1: Check deployment
        → Servers panel → Expand "Tomcat v10.1 at localhost"
        → You should see: StudentWebApp [Started, Synchronized]
        → If NOT listed: Right-click server → Add and Remove... → Add StudentWebApp → Finish
        → Right-click server → Clean → Restart the server
    
        Step 2: Check the URL
        → The correct URL is: http://localhost:8080/StudentWebApp/
        → Note the trailing slash: /StudentWebApp/  (not /StudentWebApp)
        → Try: http://localhost:8080/StudentWebApp/index.jsp
    
        Step 3: Verify files are deployed
        → File Explorer → Navigate to:
           C:\workspace-j2ee\.metadata\.plugins\org.eclipse.wst.server.core\tmp0\wtpwebapps\StudentWebApp\
        → Should contain: index.jsp and displayStudents.jsp
        → If missing: The deployment failed → Clean project → Run on Server again
```

### Possible Error D12.5: HTTP 500 — "Unable to compile class for JSP"

```
┌─────────────────────────────────────────────────────────────┐
│  HTTP Status 500 - Internal Server Error                    │
│                                                             │
│  org.apache.jasper.JasperException: Unable to compile       │
│  class for JSP                                              │
└─────────────────────────────────────────────────────────────┘

Why:    Tomcat can't compile the JSP because it can't find the Java compiler
        or the JDBC driver class.

Fix:    
    Step 1: Verify JDK in Tomcat launch configuration
        → Servers panel → Double-click "Tomcat v10.1 Server at localhost"
        → Click "Open launch configuration" (link at the bottom)
        → Classpath tab
        → Verify JRE System Library shows JDK 17
        
        → If wrong JRE: Remove the wrong one → Add the correct JDK 17
    
    Step 2: Verify JDBC driver is in the deployed application
        → File Explorer → Navigate to:
           C:\workspace-j2ee\.metadata\.plugins\org.eclipse.wst.server.core\tmp0\wtpwebapps\StudentWebApp\WEB-INF\lib\
        → Should contain: mssql-jdbc-12.8.1.jre17.jar
        → If missing: Copy the JAR there manually, or:
           → Right-click StudentWebApp → Properties → Deployment Assembly
           → Verify: WEB-INF/lib is listed and maps to /WEB-INF/lib
           → If not: Add → Java Build Path Entries → WEB-INF/lib → Finish
           → Apply → Close → Clean → Run on Server
    
    Step 3: Clean and rebuild
        → Project → Clean → Clean all projects → OK
        → Right-click StudentWebApp → Run As → Run on Server
```

### Possible Error D12.6: "javax.servlet.jsp cannot be resolved to a type"

```
Why:    The Tomcat servlet/JSP libraries are not in the build path.

Fix:    → Right-click StudentWebApp → Properties → Java Build Path
        → Libraries tab → Add Library → Server Runtime
        → Select "Apache Tomcat v10.1" → Finish
        → Apply and Close
        → Clean project → Run on Server
```

### Possible Error D12.7: Application form loads but submitting shows blank page or error

```
Why:    The displayStudents.jsp may have a compilation error or database connection issue.

Fix:    → Check the Eclipse Console panel for error messages
        → Look for: "Error" or "Exception" in the console output
        
        → Common causes:
        a) "ClassNotFound: com.microsoft.sqlserver.jdbc.SQLServerDriver"
           → JDBC driver JAR not in the deployed app
           → See D12.5 fix above
        
        b) "Login failed for user 'sa'"
           → SQL Server credentials wrong in displayStudents.jsp
           → Verify: pass = "P@ssw0rd123!" matches your SA password
           → Update the password in displayStudents.jsp → Save → Refresh browser
        
        c) "Connection refused" or "Connection to localhost:1433 failed"
           → SQL Server not running
           → Services → SQL Server (MSSQLSERVER) → Start
           → Or: TCP/IP not enabled (go back to Part B Step B6)
        
        d) "Connection to localhost:1433 failed" even though SQL Server is running
           → May be using Shared Memory only (not TCP/IP)
           → Verify: netstat -an | findstr "1433" → Must show LISTENING
           → If not: Go back to Part B Step B6
```

```
✅ CHECKPOINT: Application running in Eclipse's built-in browser
   → Registration form is visible
   → In Notepad: "✅ D12 — Application running on Tomcat via Eclipse"
```

---

## D13 — Open Application in Edge Browser

### Why This Step?

Eclipse's built-in browser works but is basic. Using Edge gives a better visual experience and lets you test the application more easily.

### What to Do

**Open Edge inside the VM:**

```
Start Menu → Type: edge → Press: Enter
```

**Navigate to:**

```
Click address bar: Ctrl + L
Type:  http://localhost:8080/StudentWebApp/
Press: Enter
```

**Expected:** The same Student Registration form loads in Edge.

### Possible Error D13.1: "This site can't be reached"

```
Why:    Tomcat isn't running or deployed on a different port.

Fix:    → Check if Eclipse's Tomcat is running:
        → Eclipse → Servers panel → Must show "Started"
        → If "Stopped": Right-click server → Start
        
        → Check the URL: Must be http://localhost:8080/StudentWebApp/
        → Note: /StudentWebApp/ with trailing slash
        
        → If you changed the port to 8081 in D12:
        → Use: http://localhost:8081/StudentWebApp/
```

```
✅ CHECKPOINT: Application loads in Edge browser inside the VM
   → In Notepad: "✅ D13 — Application accessible in Edge browser"
```

---

## D14 — Test the Application (Register a Student)

### What to Do

**In Edge inside the VM (or Eclipse browser):**

```
URL: http://localhost:8080/StudentWebApp/
```

**Fill in the registration form:**

```
First Name:    Karthik
Last Name:     Kumar
Email:         karthik.kumar@college.edu
Department:    Computer Science   (select from dropdown)
```

```
Click: "Register & View Records"
```

### Expected Result

```
┌─────────────────────────────────────────────────────────────┐
│  Student Database Records                                   │
│                                                             │
│  ✅ Student "Karthik Kumar" registered successfully!        │
│                                                             │
│  ┌───────────┐  ┌────────────┐  ┌───────────┐              │
│  │     6     │  │ SQL Server │  │    1433   │              │
│  │ Total     │  │ Database   │  │   Port    │              │
│  │ Students  │  │            │  │           │              │
│  └───────────┘  └────────────┘  └───────────┘              │
│                                                             │
│  ID │ First Name │ Last Name │ Email               │ Dept   │
│  ───┼────────────┼───────────┼─────────────────────┼─────── │
│  6  │ Karthik    │ Kumar     │ karthik@...edu      │ CS     │
│  5  │ Vikram     │ Singh     │ vikram@...edu       │ CS     │
│  4  │ Sneha      │ Reddy     │ sneha@...edu        │ Civil  │
│  3  │ Rahul      │ Patel     │ rahul@...edu        │ Mech   │
│  2  │ Priya      │ Verma     │ priya@...edu        │ Elec   │
│  1  │ Amit       │ Sharma    │ amit@...edu         │ CS     │
│                                                             │
│  [← Register Another]    [↻ Refresh]                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Verify:**
```
✅ Green success message appears
✅ Total Students shows: 6 (5 original + 1 new)
✅ The new student "Karthik Kumar" appears in the table
✅ All 5 original students are still visible
✅ Department badges are styled
✅ Buttons work (Register Another, Refresh)
```

### Possible Error D14.1: "Error: Login failed for user 'sa'"

```
Why:    The SA password in displayStudents.jsp doesn't match your actual SA password.

Fix:    → Open displayStudents.jsp in Eclipse
        → Find this line:
           String pass = "P@ssw0rd123!";
        → Change "P@ssw0rd123!" to your actual SA password from Notepad
        → Save (Ctrl + S)
        → Refresh the browser (F5)
        → Try again
```

### Possible Error D14.2: "Error: Connection refused: connect"

```
Why:    SQL Server is not running or not listening on port 1433.

Fix:    
    Step 1: Check SQL Server service
        → Inside VM → Services → SQL Server (MSSQLSERVER)
        → Must show "Running"
        → If stopped: Right-click → Start
    
    Step 2: Check TCP/IP and port
        → SQL Server Configuration Manager → Protocols → TCP/IP → Enabled
        → TCP/IP Properties → IP All → TCP Port: 1433
        → Restart SQL Server service
        → CMD: netstat -an | findstr "1433" → Must show LISTENING
    
    Step 3: Test connectivity
        → CMD: powershell -command "Test-NetConnection localhost -Port 1433"
        → Must show: TcpTestSucceeded: True
```

### Possible Error D14.3: "Error: No suitable driver found"

```
Why:    JDBC driver JAR is not in the deployed application.

Fix:    → Verify the JAR is in WEB-INF/lib:
        → Eclipse → StudentWebApp → src → main → webapp → WEB-INF → lib
        → Must show: mssql-jdbc-12.8.1.jre17.jar
        
        → If present but still fails:
        → Right-click StudentWebApp → Properties → Deployment Assembly
        → Verify: lib is listed and maps to /WEB-INF/lib
        → If not: Add → Folder → WEB-INF/lib → Finish
        → Apply → Close → Clean → Run on Server
```

### Possible Error D14.4: Form submits but the page is completely blank

```
Why:    JSP compilation error that doesn't display properly.

Fix:    → Check Eclipse Console for error messages
        → Common: NullPointerException in the JSP
        → Open displayStudents.jsp in Eclipse
        → Verify the code was pasted correctly (no missing lines)
        → If in doubt: Delete the file, create a new one, paste the code again
        → Save → Clean → Run on Server
```

### Possible Error D14.5: The old 5 records don't show (only the new one)

```
Why:    The displayStudents.jsp query might have a syntax issue.

Fix:    → This shouldn't happen with the code provided
        → If it does: Click "Refresh" button on the page
        → Or: Check SSMS → SELECT * FROM Students → Verify all records exist
```

```
✅ CHECKPOINT: Application works — student registered and displayed
   → In Notepad: "✅ D14 — Student registration tested successfully"
```

---

## D15 — Test from Your Host Machine (Laptop)

### What to Do

**Switch to Desktop 1** (`Win + Ctrl + ←`).

**Open a browser on your laptop.**

```
Type in the address bar:
http://XX.XX.XX.XX:8080/StudentWebApp/
```

**Replace `XX.XX.XX.XX`** with your VM's public IP from Notepad.

**For example:** If your VM IP is `20.123.45.67`:

```
http://20.123.45.67:8080/StudentWebApp/
```

### Expected Result

```
✅ Registration form loads from your laptop browser
✅ Form looks styled (blue gradient background, white card)
✅ All fields are interactive
```

### Register a Student from Your Laptop

```
First Name:    Priya
Last Name:     Nair
Email:         priya.nair@college.edu
Department:    Electronics

Click: "Register & View Records"
```

**Expected:**
```
✅ Green message: Student "Priya Nair" registered successfully!
✅ Total Students: 7
✅ Table shows all 7 records
```

### Possible Error D15.1: "This site can't be reached" — Timeout from laptop

```
Why:    Eclipse's Tomcat is only listening on localhost (127.0.0.1),
        not on the public network interface (0.0.0.0).

Fix:    
    Step 1: Check what Tomcat is listening on
        → Inside VM: netstat -an | findstr "8080"
        → If shows: TCP 127.0.0.1:8080 ... → Tomcat is localhost-only
        → If shows: TCP 0.0.0.0:8080 ... → Tomcat is on all interfaces (correct)
    
    Step 2: If 127.0.0.1 — Change Tomcat binding
        → Eclipse → Servers panel → Double-click "Tomcat v10.1 Server at localhost"
        → The server configuration editor opens
        → Find "HTTP/1.1" section
        → Change: "Host name" from "localhost" to "0.0.0.0"
        → OR: Leave host name and check if there's an "All Interfaces" option
        → Save (Ctrl + S)
        → Right-click server → Clean → Restart
        → Try from laptop again
    
    Step 3: If the server editor doesn't show host configuration
        → Edit the Tomcat server.xml directly:
           → File Explorer → Navigate to the Eclipse Tomcat config:
              C:\workspace-j2ee\.metadata\.plugins\org.eclipse.wst.server.core\tmp0\conf\server.xml
           → Open with Notepad
           → Find: <Connector port="8080" protocol="HTTP/1.1" ... />
           → Add: address="0.0.0.0"
              → So it becomes: <Connector port="8080" protocol="HTTP/1.1" address="0.0.0.0" ... />
           → Save → Restart Tomcat in Eclipse
           → Try from laptop again
    
    Step 4: Alternative — Use the Tomcat Windows service instead
        → Stop Eclipse's Tomcat: Right-click server → Stop
        → Start the Windows service: CMD (Admin): net start Tomcat10
        → Copy the WAR file to the service's webapps:
           → Export WAR from Eclipse (see D16 below)
           → Copy to: C:\apache-tomcat-10.1.34\webapps\
        → Try from laptop: http://XX.XX.XX.XX:8080/StudentWebApp/

    Step 5: Check Windows Firewall and Azure NSG
        → Windows Firewall: netsh advfirewall firewall show rule name="Allow Tomcat 8080"
        → If missing: netsh advfirewall firewall add rule name="Allow Tomcat 8080" dir=in action=allow protocol=TCP localport=8080
        → Azure NSG: Already configured in Part C Step C11
        → Verify: Azure Portal → VM → Networking → Port 8080 rule exists
```

### Possible Error D15.2: Form loads but submitting shows connection error

```
Why:    The database connection still uses "localhost" which works inside the VM
        but the web app is running inside the VM so it should still work.
        The issue may be SQL Server stopped or credentials changed.

Fix:    → Same as D14.1 and D14.2 above
        → This error only affects the form submission, not page loading
        → Verify SQL Server is running inside the VM
```

```
✅ CHECKPOINT: Application accessible from your laptop via public IP
   → http://XX.XX.XX.XX:8080/StudentWebApp/ works
   → Student registration works from laptop
   → In Notepad: "✅ D15 — Application accessible from laptop"
```

---

## D16 — Verify Data in SSMS

### What to Do

**Switch to Desktop 2** (inside the VM).

**Open SSMS** (it may still be open from Part B):

```
If closed: Start Menu → Search: SSMS → Open
Server: localhost
Auth: SQL Server Authentication
Login: sa / Password: P@ssw0rd123!
Click: Connect
```

**Click: New Query**

**Paste and execute:**

```sql
USE StudentDB;
GO

SELECT * FROM Students ORDER BY StudentID;
GO
```

**Expected Results:**

```
┌──────────┬───────────┬──────────┬──────────────────────────────────┬─────────────────────┬─────────────────────┐
│StudentID │ FirstName │ LastName │ Email                            │ Department          │ CreatedDate         │
├──────────┼───────────┼──────────┼──────────────────────────────────┼─────────────────────┼─────────────────────┤
│    1     │ Amit      │ Sharma   │ amit.sharma@college.edu          │ Computer Science    │ 2025-xx-xx xx:xx:xx │
│    2     │ Priya     │ Verma    │ priya.verma@college.edu          │ Electronics         │ 2025-xx-xx xx:xx:xx │
│    3     │ Rahul     │ Patel    │ rahul.patel@college.edu          │ Mechanical          │ 2025-xx-xx xx:xx:xx │
│    4     │ Sneha     │ Reddy    │ sneha.reddy@college.edu          │ Civil               │ 2025-xx-xx xx:xx:xx │
│    5     │ Vikram    │ Singh    │ vikram.singh@college.edu         │ Computer Science    │ 2025-xx-xx xx:xx:xx │
│    6     │ Karthik   │ Kumar    │ karthik.kumar@college.edu        │ Computer Science    │ 2025-xx-xx xx:xx:xx │
│    7     │ Priya     │ Nair     │ priya.nair@college.edu           │ Electronics         │ 2025-xx-xx xx:xx:xx │
└──────────┴───────────┴──────────┴──────────────────────────────────┴─────────────────────┴─────────────────────┘

(7 rows affected)
```

**Verify:**
```
✅ 7 total records (5 original + 2 registered via web form)
✅ Karthik Kumar (ID 6) registered from inside the VM
✅ Priya Nair (ID 7) registered from your laptop
✅ CreatedDate timestamps are correct
✅ All departments are correct
```

```
✅ CHECKPOINT: Data verified end-to-end in SSMS
   → Browser → JSP → JDBC → SQL Server → Data stored and retrieved
   → In Notepad: "✅ D16 — Data verified in SSMS (7 records)"
```

---

## D17 — Export WAR File (Optional — For Manual Deployment)

### Why Export?

If you want to deploy the application without Eclipse (e.g., after the lab, or if Eclipse crashes), you can export a `.war` file and deploy it manually to Tomcat.

### What to Do

**In Eclipse:**

```
Right-click on: StudentWebApp
→ Export...
→ Web → WAR file → Next
```

```
┌─────────────────────────────────────────────────────────────┐
│  WAR Export                                                 │
│                                                             │
│  Web project:        StudentWebApp                          │
│                                                             │
│  Destination:                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  C:\apache-tomcat-10.1.34\webapps\StudentWebApp.war│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ☑ Overwrite existing file                                  │
│                                                             │
│            [ Finish ]    [ Cancel ]                          │
└─────────────────────────────────────────────────────────────┘
```

```
Web project:    StudentWebApp (auto-filled)
Destination:    C:\apache-tomcat-10.1.34\webapps\StudentWebApp.war

Click: Finish
```

### Deploy WAR Manually

```cmd
:: Stop Eclipse's Tomcat (if running)
:: (In Eclipse: Right-click server → Stop)

:: Start the Windows service
net start Tomcat10

:: Tomcat auto-deploys WAR files in webapps/
:: Verify deployment:
dir C:\apache-tomcat-10.1.34\webapps\StudentWebApp
```

**Access the manually deployed application:**

```
http://localhost:8080/StudentWebApp/
```

```
✅ CHECKPOINT: WAR file exported and deployed to Tomcat Windows service
   → Application accessible via both Eclipse and Windows service
```

---

## D18 — Final Complete Verification

### Run All Tests

**Inside the VM**, open Command Prompt:

```cmd
echo =============================================
echo     PART D - FULL VERIFICATION
echo =============================================

echo.
echo --- Test 1: Eclipse IDE ---
dir C:\eclipse\eclipse.exe

echo.
echo --- Test 2: Dynamic Web Project ---
dir C:\workspace-j2ee\StudentWebApp\src\main\webapp\index.jsp
dir C:\workspace-j2ee\StudentWebApp\src\main\webapp\displayStudents.jsp

echo.
echo --- Test 3: JDBC Driver ---
dir C:\workspace-j2ee\StudentWebApp\src\main\webapp\WEB-INF\lib\mssql-jdbc-12.8.1.jre17.jar

echo.
echo --- Test 4: web.xml ---
dir C:\workspace-j2ee\StudentWebApp\src\main\webapp\WEB-INF\web.xml

echo.
echo --- Test 5: Tomcat (Eclipse or Service) ---
sc query Tomcat10
echo OR check Eclipse Servers panel

echo.
echo --- Test 6: Port 8080 ---
netstat -an | findstr "8080"

echo.
echo --- Test 7: SQL Server ---
netstat -an | findstr "1433"

echo.
echo --- Test 8: Application URL ---
powershell -command "try { $r = Invoke-WebRequest -Uri 'http://localhost:8080/StudentWebApp/' -UseBasicParsing; Write-Host 'HTTP Status:' $r.StatusCode; Write-Host 'Content contains Student:' ($r.Content -match 'Student Registration') } catch { Write-Host 'FAILED:' $_.Exception.Message }"

echo.
echo =============================================
echo     ALL TESTS COMPLETE
echo =============================================
```

### Expected Results

```
=============================================
    PART D - FULL VERIFICATION
=============================================

--- Test 1: Eclipse IDE ---
C:\eclipse\eclipse.exe  ✅ EXISTS

--- Test 2: Dynamic Web Project ---
C:\workspace-j2ee\StudentWebApp\src\main\webapp\index.jsp  ✅ EXISTS
C:\workspace-j2ee\StudentWebApp\src\main\webapp\displayStudents.jsp  ✅ EXISTS

--- Test 3: JDBC Driver ---
C:\workspace-j2ee\...\mssql-jdbc-12.8.1.jre17.jar  ✅ EXISTS

--- Test 4: web.xml ---
C:\workspace-j2ee\...\WEB-INF\web.xml  ✅ EXISTS

--- Test 5: Tomcat ---
STATE: 4  RUNNING  ✅

--- Test 6: Port 8080 ---
TCP    0.0.0.0:8080    0.0.0.0:0    LISTENING  ✅

--- Test 7: SQL Server ---
TCP    0.0.0.0:1433    0.0.0.0:0    LISTENING  ✅

--- Test 8: Application URL ---
HTTP Status: 200
Content contains Student: True  ✅

=============================================
    ALL TESTS COMPLETE
=============================================
```

```
✅ CHECKPOINT: All 8 verification tests passed
   → In Notepad: "✅ D18 — All Part D tests passed"
```

---

## Part D Final Summary

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│              PART D — COMPLETE                              │
│                                                             │
│  ✅ D1   Eclipse IDE (Enterprise Java) downloaded           │
│  ✅ D2   Eclipse extracted to C:\eclipse                    │
│  ✅ D3   Eclipse launched, workspace set                    │
│  ✅ D4   Tomcat v10.1 configured in Eclipse                 │
│  ✅ D5   JDBC Driver 12.8.1 downloaded and extracted        │
│  ✅ D6   Dynamic Web Project "StudentWebApp" created        │
│  ✅ D7   JDBC Driver JAR added to WEB-INF/lib              │
│  ✅ D8   index.jsp (registration form) created              │
│  ✅ D9   displayStudents.jsp (records display) created      │
│  ✅ D10  Build path configured (if needed)                  │
│  ✅ D11  Tomcat Windows service stopped                     │
│  ✅ D12  Application deployed and running on Tomcat         │
│  ✅ D13  Application accessible in Edge browser             │
│  ✅ D14  Student registration tested (inside VM)            │
│  ✅ D15  Application accessible from laptop                 │
│  ✅ D16  Data verified in SSMS                              │
│  ✅ D17  WAR file exported (optional)                       │
│  ✅ D18  All verification tests passed                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

# COMPLETE

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│              🎉  SUCCESSFULLY COMPLETED  🎉                │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  Azure VM with SQL Server and J2EE Platform to Host   │ │
│  │  Web Applications                                     │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  ✅ Part A — Azure VM (Windows Server 2022)                │
│  ✅ Part B — SQL Server 2022 (StudentDB)                   │
│  ✅ Part C — JDK 17 + Apache Tomcat 10.1                   │
│  ✅ Part D — Eclipse IDE + J2EE Web Application            │
│                                                             │
│  ──────────────────────────────────────────────────────────│
│  ARCHITECTURE                                              │
│  ──────────────────────────────────────────────────────────│
│                                                             │
│  ┌──────────┐     HTTP      ┌────────────────────────────┐ │
│  │  Laptop  │──────────────▶│  Azure VM                  │ │
│  │  Browser │  Port 8080    │  ┌──────────────────────┐  │ │
│  └──────────┘               │  │ Apache Tomcat 10.1.34│  │ │
│                             │  │ Port 8080            │  │ │
│                             │  │                      │  │ │
│                             │  │ StudentWebApp.war    │  │ │
│                             │  │ ├─ index.jsp         │  │ │
│                             │  │ └─ displayStudents.. │  │ │
│                             │  └──────────┬───────────┘  │ │
│                             │             │ JDBC          │ │
│                             │  ┌──────────▼───────────┐  │ │
│                             │  │ SQL Server 2022      │  │ │
│                             │  │ Port 1433            │  │ │
│                             │  │ Database: StudentDB  │  │ │
│                             │  │ Table: Students      │  │ │
│                             │  └──────────────────────┘  │ │
│                             └────────────────────────────┘ │
│                                                             │
│  ──────────────────────────────────────────────────────────│
│  TECHNOLOGIES USED                                         │
│  ──────────────────────────────────────────────────────────│
│  • Azure Virtual Machine (Standard_B2s)                    │
│  • Windows Server 2022 Datacenter                          │
│  • Microsoft SQL Server 2022 Developer                     │
│  • SQL Server Management Studio 21                         │
│  • Eclipse Temurin JDK 17.0.13 (LTS)                      │
│  • Apache Tomcat 10.1.34                                   │
│  • Eclipse IDE 2024-09 (Enterprise Java & Web)             │
│  • JSP (JavaServer Pages)                                  │
│  • JDBC (Java Database Connectivity)                       │
│  • Microsoft JDBC Driver 12.8.1                            │
│  • HTML/CSS                                                │
│  • Jakarta EE 9 (Servlet 5.0, JSP 3.0)                     │
│                                                             │
│  ──────────────────────────────────────────────────────────│
│  URLS                                                      │
│  ──────────────────────────────────────────────────────────│
│  Inside VM:   http://localhost:8080/StudentWebApp/          │
│  From Laptop: http://XX.XX.XX.XX:8080/StudentWebApp/       │
│  Manager:     http://localhost:8080/manager/html            │
│  SSMS:        localhost, SQL Auth, sa / P@ssw0rd123!       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Final Notepad State

Your Desktop 1 Notepad should look like this:

```
============================================
     MASTER CREDENTIALS
============================================

AZURE PORTAL
  URL:            https://portal.azure.com
  Email:          (your email)
  Password:       (your password)

RESOURCE GROUP
  Name:           RG-Exp6-J2EE
  Region:         Central India (or your region)

VIRTUAL MACHINE
  Name:           VM-J2EE-SQL
  Public IP:      XX.XX.XX.XX
  Username:       azureadmin
  Password:       (your VM password)

WINDOWS FIREWALL (inside VM)
  Port 8080:      Allowed
  Port 1433:      Allowed
  Port 1434:      Allowed

SQL SERVER 2022
  Edition:        Developer
  SA Username:    sa
  SA Password:    P@ssw0rd123!
  Database:       StudentDB
  Table:          Students (7 records)
  SQL Port:       1433
  TCP/IP:         Enabled
  NSG Rule:       Allow_SQL_1433 (priority 310)

JDK 17
  Version:        Temurin 17.0.13 (LTS)
  JAVA_HOME:      C:\Program Files\Eclipse Adoptium\jdk-17.0.13.11-hotspot

APACHE TOMCAT 10.1
  Version:        10.1.34
  Home:           C:\apache-tomcat-10.1.34
  Port:           8080
  Service:        Tomcat10
  Admin User:     admin
  Admin Pass:     tomcat123
  Manager URL:    http://localhost:8080/manager/html
  NSG Rule:       Allow_Tomcat_8080 (priority 320)

ECLIPSE IDE
  Version:        2024-09 (Enterprise Java & Web)
  Location:       C:\eclipse\eclipse.exe
  Workspace:      C:\workspace-j2ee
  Project:        StudentWebApp

JDBC DRIVER
  Version:        12.8.1
  JAR File:       mssql-jdbc-12.8.1.jre17.jar
  Location:       C:\JDBC\sqljdbc_12.8\enu\

WEB APPLICATION
  URL (VM):       http://localhost:8080/StudentWebApp/
  URL (Laptop):   http://XX.XX.XX.XX:8080/StudentWebApp/
  Form:           index.jsp
  Results:        displayStudents.jsp

============================================
  ALL PARTS COMPLETE ✅
  ✅ Part A — Azure VM
  ✅ Part B — SQL Server
  ✅ Part C — JDK + Tomcat
  ✅ Part D — Eclipse + Web App
============================================
```

---

## Cleanup After Experiment

### Option A: Stop VM (Resume Later)

```
Azure Portal → Virtual Machines → VM-J2EE-SQL → Stop

You will NOT be charged for compute while the VM is stopped.
You WILL be charged a small amount for disk storage (~$1.50/month).
```

### Option B: Delete Everything (Complete Cleanup)

```
Azure Portal → Resource Groups → RG-Exp6-J2EE → Delete Resource Group
→ Type the resource group name to confirm → Delete

This deletes: VM, disks, networking, public IP — everything.
You will be charged only for the time the resources existed.
```

**Estimated total cost for the experiment (if cleaned up same day): $1–3**

---

# PART - D COMPLETED SUCCESSFULLY

